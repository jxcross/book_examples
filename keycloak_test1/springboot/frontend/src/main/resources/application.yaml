server:
  port: 3003
spring:
  security:
    oauth2:
      client:
        registration:
          myfrontend:
            provider: keycloak
            client-id: test_site_a
            # change CLIENT_SECRET below with the secret generated by Keycloak for the mywebapp client
            client-secret: kEFr7BhHOi8J4MNoh2C6kKpYptUNcXf1
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/"
            scope: openid
        provider:
          keycloak:
            issuer-uri: https://dev248.edison.re.kr:8543/auth/realms/EDISON2
