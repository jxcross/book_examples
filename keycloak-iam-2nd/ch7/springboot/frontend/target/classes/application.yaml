server:
  port: 58080
spring:
  security:
    oauth2:
      client:
        registration:
          myfrontend:
            provider: keycloak
            client-id: mywebapp
            # change CLIENT_SECRET below with the secret generated by Keycloak for the mywebapp client
            client-secret: 6ve1B4Z3tzoJsSiC5OCaNQZlFWHLlVRC
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/"
            scope: openid
        provider:
          keycloak:
            issuer-uri: http://localhost:58081/realms/myrealm